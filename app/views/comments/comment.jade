
if (comment && comment.user)
  .comment
    a(href="/users/"+comment.user._id) #{comment.user.name}
    |  :&nbsp;
    != comment.body
    br
    span.date #{formatDate(comment.createdAt, "LLL ")}
